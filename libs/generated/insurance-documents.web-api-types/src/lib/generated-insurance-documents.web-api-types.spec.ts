import { generatedInsuranceDocumentsWebApiTypes } from './generated-insurance-documents.web-api-types';

describe('generatedInsuranceDocumentsWebApiTypes', () => {
  it('should work', () => {
    expect(generatedInsuranceDocumentsWebApiTypes()).toEqual(
      'generated-insurance-documents.web-api-types'
    );
  });
});
